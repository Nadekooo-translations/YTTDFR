---
const { class: clazz, ...rest } = Astro.props;
const hash = import.meta.env.COMMIT_HASH;
---

<iframe
	class={clazz}
	src={`/yttd-${hash}/`}
	allow="autoplay fullscreen"
	allowfullscreen="true"
	width="816"
	height="624"
	{...rest}
>
</iframe>
