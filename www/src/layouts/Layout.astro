---
import BaseLayout from "./BaseLayout.astro";
import "../styles/main.scss";
import Navbar from "../components/Navbar.astro";

const { title, description, heroUrl, activeNav, extraSchemas, extraBreadcrumbs } = Astro.props;
---

<BaseLayout {title} {description} {heroUrl} {extraSchemas} {extraBreadcrumbs}>
	<main>
		<Navbar {activeNav} />
		<slot />
	</main>
</BaseLayout>
